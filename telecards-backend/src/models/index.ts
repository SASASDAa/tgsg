import PlayerModel from './Player';
import CardDefinitionModel from './Card';
// Import other models as they are created (e.g., DeckModel, FriendRequestModel)

export {
  PlayerModel,
  CardDefinitionModel,
};

console.log("All models indexed (mock)");
